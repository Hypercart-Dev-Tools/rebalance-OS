---
title: "EOD 2026-07-22 — 3-Eyes activation, doc-governance reconciliation, GH-198 Phase 0"
status: "Complete — iteration wrapped; delivered direct to development"
created: 2026-07-22
updated: 2026-07-22
owner: Noel
doc_type: note
roadmap_exempt: true
goal: >
  Record what shipped on 2026-07-22, what carries forward, and the delivery state
  of the iteration, so the next session can resume without re-deriving it.
---

# EOD — 2026-07-22

**Day window:** 2026-07-22 00:00 → 21:xx PDT. First EOD summary in this repo; window fell back to today 00:00 local.

## What shipped

Seventeen commits landed on `development` today, across three efforts:

- **GH-195 — 3-Eyes unified local job supervisor.** Built, Codex-reviewed over two rounds, merged via PR #196, and **activated on the Mac Studio** managing `skill-sync` beside the `selfcheck` demo job. Three health-honesty fixes followed the same day (0.67.0), all found by running the thing against its own fleet: a live `pulse-server` reported `FAIL(exit -15)` because health read `launchctl list`'s status column and ignored the PID column; an unreachable `launchctl` silently read as "everything not-loaded" and now reports `unknown`; and the Focus 5 tile, which keyed on `failing == 0`, rendered that unreadable state as "all jobs OK". A fail-closed `supersedes` adoption guard now stops `install` from standing a managed job up beside the incumbent it replaces. 93 tests green.
- **GH-190 — `/rebalance` live-signal skill.** Merged via PR #191, then PR #192 flattened the skill directory so it actually registers as `/rebalance`. Issue closed.
- **GH-198 — local Qwen embedding proof.** Phase 0 passed; see below.

Two doc-reconciliation commits also landed: `ebca27b` closed all 30 deterministic PDDA doc errors, and `a1f8321` corrected the GH-154 "unmerged" claim (the theme picker shipped 2026-07-19 via PR #163).

## The wrap itself

- **GH-146 archived.** Issue #146 has been closed since 2026-07-18, but its whole effort tree was still sitting in `2-WORKING`. Moved to `PROJECT/3-COMPLETED/GH-146-HEALTH-SIGNAL-ACCURACY/` (4 briefs + 2 MARATHON yamls, kept together).
- **ROADMAP corrected.** The Status cell still claimed "**30 PDDA doc errors remain**" and enumerated them, hours after `ebca27b` closed all 30. Replaced with the verified `0 errors across all 7 checks`, plus a note that PR #194 is open and unmerged.
- **GH-198 doc created** at `PROJECT/2-WORKING/GH-198-LOCAL-QWEN-EMBEDDING-PROOF.md`, with a matching ROADMAP pointer.
- **`gh-refresh` run** — 111 issue states cached, which resolved the one `issue-doc-sync` finding that had been reporting "state unavailable — sync NOT evaluated" rather than passing.

`issue-doc-sync` went **21 warns → 16**. Deterministic errors stayed at 0.

## GH-198 — Phase 0 passed, deliberately not merged

The MLX Qwen runtime projected, embedded, and cold-queried a 1,560-document RebalanceOS `code` corpus against a gitignored staging DB: 222.1s, 1.468 GiB peak tree RSS, 1,560/1,560 vectors all stamped `Qwen/Qwen3-Embedding-0.6B|1024`, plus a verified one-row stale-model repair. Production `rebalance.db` stayed at 0 documents / 0 vectors. Only environment constraint: headless sandboxes cannot reach Metal. **No local-embedding blocker.**

The Phase 0 branch `codex/gh-198-local-qwen-embedding-spike` (`29dbd93`) was **not merged and was torn down**. It changed no code, wrote only to a gitignored staging DB, and its evidence already lives on the issue — merging it would have created a `2-WORKING` doc against a closed issue, the exact drift class this wrap cleaned up. Issue #198 was reopened to carry Phases 1–2, and the scope was posted there as a comment.

**Spun out: [#201](https://github.com/Hypercart-Dev-Tools/rebalance-OS/issues/201)** — `resolve_database_path()` selects the first *existing* candidate, so an explicit `--database` pointing at a not-yet-created path silently falls through to the canonical app-data DB. This actually happened during Phase 0: 1,560 documents were projected into the canonical DB before anyone noticed. Confirmed in code at `src/rebalance/paths.py:177,190-192`.

## Two honesty notes worth keeping

- **`rebalance doctor` reported 8 scheduler jobs as "not loaded on this device."** That was a sandbox misread, not a fleet failure — unsandboxed, `launchctl list` returns 531 jobs with all of them present. Same bug class as the GH-195 fixes shipped today, reproducing in `doctor`.
- **A stale worktree produced a false finding mid-wrap.** After `cd`-ing into `/private/tmp/rebalance-os-gh-198` to inspect the spike commit, subsequent relative-path shell commands read that worktree instead of the main repo — which branched before `a1f8321`. This produced a confident but wrong claim that 6 GH-154 docs still said "branch unmerged" (they don't; `a1f8321` fixed them), and put one `git mv` in the wrong tree. Both were caught and reversed; the worktree was restored clean at `29dbd93` with nothing lost. **Lesson: `cd` into a linked worktree and every later relative path silently retargets.** Prefer absolute paths, or `cd` back explicitly.

## What's next

- **PR #194 (GH-193, optional PDDA lifecycle signal)** — open, mergeable, checks green (3.12 + 3.13), awaiting review. Pushed, **not shipped**. Its ROADMAP ledger entry lands when it merges.
- **GH-198 Phase 1** — reproducible stage-owned smoke test + runtime metrics through the orchestrator/`doctor` path. Phase 2 (rollout decision, and whether ask-self stays Gemini-backed) is gated behind it.
- **#201** — fix the `--database` precedence so an explicit target never degrades to the canonical DB.
- **3-Eyes adoption** — git-pulse, Cactus-Needle/Sleuth, and the XYZ-debug sentinel are still observed-not-managed; `collector-health` stays registered-but-uninstalled until the `health-check*` incumbents retire.
- **GH-169** commit-history backfill (plan approved, no code written) and **MARATHON-2026-07-21** (4 disjoint fixes, planned, never fired) both still have real un-started scope.
- **GH-154** carries three post-merge items before it archives: legacy-alias retirement, a true first-paint (CDP) check, and the operator visual pass on the light theme.

## Open findings carried forward

- **16 `issue-doc-sync` warns remain.** Eight are docs in `3-COMPLETED` whose issues are still OPEN (#112, #114, #120, #121, #130, #131, #135, #156) — closure candidates. Six are GH-154 docs reading `Complete`/`Shipped` while #154 stays open for its post-merge items; those docs are factually accurate, so this is checker granularity (a phase brief compared against parent-issue state), not doc drift.
- **16 `stale-working-docs` warns** across long-lived `2-WORKING` docs. Untriaged.
- **`PROJECT/1-INBOX/GH-NEXT-SIMPLIFY-REPO-INTAKE.md` is untracked** and was drafted during this session by the 3-Eyes `pdda-inbox` route (`gh_issue: NEXT`, `status: Proposed`). It is **deliberately excluded** from this commit — it awaits human promotion, which is the route's designed behavior. While it sits untracked it produces one `roadmap-coverage` ERROR, since it carries a `gh_issue:` key but no ROADMAP park. Promote it or set `roadmap_exempt: true`.
- **`signal health — figma: last refresh advanced 42d ago`** (window 7d) — the one genuine `doctor` warn.
- **Two stashes** predate today and are shared across worktrees: `stray /.tick gitignore entry (pre-#59-rebase)` and `WIP whats-next/next-actions (focus5)`.

## Delivery state

Committed to `development` and pushed directly (remote default branch, unprotected, clean, in sync). PR #194 remains a separate pending checkpoint and was not touched by this wrap.
