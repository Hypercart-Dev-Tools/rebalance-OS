1. High: “active phase” is derived as the lowest-numbered incomplete phase, not the current execution phase, and stall detection inherits that scope. In [plan_progress.py](/Users/noelsaw/Documents/rebalance-OS/PROJECT/plan_progress.py:241), `compute_metrics()` picks `incomplete[0]` after sorting by phase number; then [plan_progress.py](/Users/noelsaw/Documents/rebalance-OS/PROJECT/plan_progress.py:389) only flags `open` tasks from that phase as stalled. In the example, the generated block says active is Phase 1 [plan_example.md](/Users/noelsaw/Documents/rebalance-OS/PROJECT/plan_example.md:21), while the document itself says “What’s next” is Phase 5 [plan_example.md](/Users/noelsaw/Documents/rebalance-OS/PROJECT/plan_example.md:29). That means Phase 5 tasks can age indefinitely without ever entering the stalled bucket as long as an older carryover task remains open.

2. Medium: the state log key is just task text, so duplicate task titles across phases collide. [plan_progress.py](/Users/noelsaw/Documents/rebalance-OS/PROJECT/plan_progress.py:140) defines the key as `text.strip()`, [plan_progress.py](/Users/noelsaw/Documents/rebalance-OS/PROJECT/plan_progress.py:270) stores state by that key, and [plan_progress.py](/Users/noelsaw/Documents/rebalance-OS/PROJECT/plan_progress.py:373) dedupes stall reporting with the same key. If two phases both contain something like “Update docs” or “Add tests”, one task’s status/age will overwrite the other and one of them will disappear from stall reporting.

3. Medium: any `###` inside a phase permanently switches the parser into “skip tasks” mode, even if it is not the QA section. In [plan_progress.py](/Users/noelsaw/Documents/rebalance-OS/PROJECT/plan_progress.py:181), every `###` sets `in_qa = True`, and there is no exit path until the next `##`. So a future phase layout like `### Risks`, `### Follow-ups`, or `### Phase 3A` would silently drop every checkbox that follows from metrics, state, and stalls. It works on the current example only because `### QA Checklist` is effectively the last subheader in each phase.

4. Low: the self-referential command paths are wrong. The usage text [plan_progress.py](/Users/noelsaw/Documents/rebalance-OS/PROJECT/plan_progress.py:36) and generated note [plan_progress.py](/Users/noelsaw/Documents/rebalance-OS/PROJECT/plan_progress.py:61) both tell users to run `scripts/plan_progress.py`, but the file is actually `PROJECT/plan_progress.py`. The example already embeds that incorrect regenerate command [plan_example.md](/Users/noelsaw/Documents/rebalance-OS/PROJECT/plan_example.md:19).

Open question: should `deferred` tasks count against phase closure and overall progress, or should they be excluded like `cancelled`? The current implementation counts them in totals while merely hiding them from stall reporting, which may or may not match the intended semantics.

Aside from those issues, the overall shape is solid: single-owner markers, in-file state instead of git, and `--check`/`--print` behavior are pragmatic. I ran it against `PROJECT/plan_example.md`; it exits cleanly today, so these are semantic correctness risks rather than immediate runtime failures.

If you want, I can turn this into a concrete patch next and keep the fixes narrow.