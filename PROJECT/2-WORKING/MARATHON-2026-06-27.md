---
title: Parallel Build Queue — 2026-06-27
status: Active
owner: noel
roadmap_exempt: true
created: 2026-06-27
updated: 2026-06-27
goal: >
  High-level automation plan: which in-progress ROADMAP items still have real
  build work, which are done-pending-verification, and which of the buildable
  ones can run simultaneously (XYZ path-scoped lanes) without file collisions.
---

# Parallel Build Queue — 2026-06-27

> Planning artifact, not a project doc. Built from the **actual** state of each
> `PROJECT/2-WORKING/**` doc (the ROADMAP ledger was stale on several items —
> see [§ROADMAP drift](#roadmap-drift)).

## Status

| What was just completed | What's next |
|---|---|
| Queue authored and corrected to **verified** doc state (2026-06-27); 3 buildable lanes identified as path-disjoint; promoted into `2-WORKING`, internal links fixed. `roadmap_exempt` (dated planning artifact, not a ledger project). | Launch lanes A/B/C via `tick` (`/xyz`), or run the ROADMAP ledger refresh first. |

## TL;DR

Only **3 items still have real build work**, and after the Refresh/Restart v1
trim they are **fully path-disjoint** — all three can run at once (XYZ-safe):

1. **SWIFT-APP** — VS Code focus-if-open **Phase 1** (Focus5Float Mac app)
2. **BACKEND** — Unified Refresh/Restart **v1** (signed helper + pulse server)
3. **SPIKE** — Focus 5 App Store **Phase 0** (isolated new track, read-mostly)

Everything else is **done or done-pending-operator-litmus** — not build lanes
(see [§Verify & archive](#verify--archive-sweep-operator-not-agent)).

## What is actually buildable

| Item | True state | Write-glob (lane) | Lane |
|---|---|---|---|
| VS Code focus-if-open **Phase 1** | not started | `macOS/Apps/Focus5Float/Sources/Focus5Float/**` | SWIFT-APP |
| Unified Refresh/Restart **v1** (3 edits) | not started; Focus 5 wiring **cut** from v1 | `scripts/apple_reminders_helper_app.swift`, `scripts/pulse_server.py`, `scripts/pulse_web.py` (column render) | BACKEND |
| Focus 5 App Store **Phase 0** spike | not started | new `macOS/Apps/Focus5Native/**` + read-only recon | SPIKE |

**Why these three are collision-free:** the Refresh/Restart **v1 trim** deferred
the Focus 5 Swift wiring, so BACKEND now touches only the `scripts/` helper +
pulse server — **not** the Focus5Float app. SWIFT-APP owns the Focus5Float
sources; SPIKE owns a brand-new dir. Three disjoint claims, zero shared files.
No migrations added by any lane this round.

## Safe concurrent set (launch all three)

### Lane A — SWIFT-APP · VS Code focus-if-open Phase 1
- **Claim:** `macOS/Apps/Focus5Float/Sources/Focus5Float/**`
- Swap `NSWorkspace.open(vscode://)` for a direct-argv `Process` running `code <repoPath>`, with a `vscode://` fallback. Easy-revert.
- → [VSCODE-OPEN-WORKSPACE.md](VSCODE-OPEN-WORKSPACE.md)

### Lane B — BACKEND · Unified Refresh/Restart v1
- **Claim:** `scripts/apple_reminders_helper_app.swift`, `scripts/pulse_server.py`, `scripts/pulse_web.py`
- v1 = 3 edits: helper `list-active` op → `/api/refresh` reads it (atomic, last-good-wins, ~5s timeout) → reminders column renders it. No FDA (uses the shipped EventKit helper grant).
- → [UNIFIED-REFRESH-RESTART.md](UNIFIED-REFRESH-RESTART.md)

### Lane C — SPIKE · Focus 5 App Store Phase 0
- **Claim:** new `macOS/Apps/Focus5Native/**` + read-only recon (no writes outside the new dir)
- Prove sandboxed folder pick, security-scoped bookmark restore, native repo probe, and the `Process`-vs-embedded-git decision. Investigation + throwaway prototype.
- → [FOCUS-5-APP-STORE.md](FOCUS-5-APP-STORE.md)

**Note — the helper is shared infrastructure.** Lane B edits the *signed Apple
Reminders helper* (`scripts/apple_reminders_helper_app.swift`), which Apple
Reminders Phase 5.1 also owns. Apple Reminders is **done**, so no concurrent
writer — but if you reopen any Apple Reminders write work, it collides with
Lane B. One writer to the helper at a time.

## Do not parallelize

- **VS Code Phase 2 (web)** — gated in its own plan (build the localhost exec
  endpoint only if `vscode://` is actually annoying) **and** would touch the
  pulse web surface Lane B is editing. Defer; don't lane it this round.
- **A second writer to the signed helper** — Lane B owns it this round.

## Verify & archive sweep (operator, not agent)

These have **no build work left** — they need a human to launch the `.app` and
eyeball the result. Per the Apple Reminders TCC findings, **no CLI agent can
satisfy a LaunchServices-launched GUI litmus**, so these are operator tasks, not
loop sessions. The repo moves (`git mv … 3-COMPLETED`) an agent can do once you
confirm.

- [ ] **Telemetry tab** — launch app → 📊 tab → confirm 3 demo rows green/orange/red → move doc to `3-COMPLETED`. _(code-complete, build green)_ → [P2-FOCUS5-TELEMETRY-TAB.md](P2-FOCUS5-TELEMETRY-TAB.md)
- [ ] **Offline resilience** — cold-launch offline with `pipx` binary → "Start server" → roster appears, no terminal → archive. → [P3-FOCUS5-FLOAT-OFFLINE-RESILIENCE.md](P3-FOCUS5-FLOAT-OFFLINE-RESILIENCE.md)
- [ ] **Focus5 Float (P2-MACOS)** — drop the `.icns`, then move to `3-COMPLETED`. → [P2-MACOS-FOCUS5-FLOAT.md](P2-MACOS-FOCUS5-FLOAT.md)
- [ ] **Apple Reminders** — functionally complete (Phases 0–5 shipped, 57 tests). Ship/review; optional follow-ups only (cross-version validation on a 2nd Mac, snapshot perf, notes/sections decode, Phase 6 dashboard write-back). → [APPLE-REMINDERS-UNIFIED-PLAN.md](APPLE-REMINDERS-UNIFIED-PLAN.md)

## ROADMAP drift

Found while building this queue — the ledger summaries are behind reality:

- **Apple Reminders** ledgered as "Phases 0+1 complete, **Next: Phase 2**" — actually **Phases 0–5 shipped** (write surface live-verified 2026-06-27). Badly stale.
- **Telemetry / Offline / P2-MACOS Focus5 Float** ledgered as in-progress — all **code-complete**, only operator litmus / `.icns` / archive remain.
- `roadmap-coverage` passes because it only checks a *link* exists, not that the *summary* is true.

→ Recommend a ROADMAP ledger refresh (move the done items to **Completed**, fix the Apple Reminders line). Separate task — flagged, not done here.

## Hard invariants (carry into every lane prompt)

- [ ] Lanes write **only** within their declared claim glob — a write outside it is a collision; abort and re-scope.
- [ ] **One writer** to `scripts/apple_reminders_helper_app.swift` (Lane B) and to `Focus5Float/Sources/**` (Lane A) this round.
- [ ] Run the per-lane check before claiming done: Lane A `swift build` + `FOCUS5_SELFTEST=1`; Lane B `pytest` + a live `/api/refresh` hit; Lane C build the spike + write the git-impl decision.
- [ ] Each lane updates its own plan's status table + `updated:` on finish; this queue is updated only when a lane closes.

## How to launch

- **Concurrent build lanes (A/B/C):** one `tick` claim per glob above (`/xyz`), a build agent per lane. Disjoint claims ⇒ collision-free.
- **Verify sweep:** operator runs the GUI litmus items; an agent does the `git mv … 3-COMPLETED` archive moves on your go.
