# Memory

- Calendar viewer "upcoming" queries must compare offset-aware event starts by absolute UTC time, not raw ISO text; same-day Pacific morning events sort before UTC afternoon strings even when they are still upcoming.
- Phase 1 Gmail ingest uses Google Application Default Credentials (`gcloud auth application-default login` with `gmail.readonly`), stores subject + Gmail snippet only, and can be narrowed via `gmail_query_filter` in `temp/rbos.config`.
- Operator-local Gmail auth now uses the shared Workspace Desktop OAuth client at `~/secrets/google-workspace-oauth-client.json`, with ADC cached at `~/.config/gcloud/application_default_credentials.json`; this survives reboot and is documented in `~/bin/servers.md` plus `~/secrets/google-calendar.env`.
- The intended optional Secret Manager source of truth for the Workspace OAuth client is `google-workspace-oauth-client` in project `named-equator-493617-e5`; local refresh writes back to `~/secrets/google-workspace-oauth-client.json`.
- Noel's current operator-local Gmail ingest filter is `in:inbox is:starred is:important`; the product default remains `in:inbox`, and narrowing the filter requires a one-time manual cleanup if the local DB should drop older broader-scope email rows.
- The local pulse web UI now keeps up to 3 recent goal completions in `temp/pulse_goal_history.json` and supports up to 3 step undo via `/api/goals/undo`; the Goals file remains the single source of truth and undo replays against the actual markdown checkbox lines.
- The current-state Gmail project doc now lives at `PROJECT/1-INBOX/EMAIL-INGEST.md`; keep it aligned with shipped behavior on `main`, especially the Workspace custom OAuth route.
- Bash-script RAG spike entrypoint: `temp/bash_script_rag_spike.py`.
- Reusable spike artifacts: `temp/rag/bash-script-spike.sqlite` and `temp/logs/bash-script-spike.jsonl`.
- The spike's default extra corpus includes wp-code-check scanner patterns from `../GH Repos/wp-code-check/dist/patterns/**/*.json` plus `PATTERN-LIBRARY.json` and `PATTERN-LIBRARY.md`.
- Repo direction is pivoting from a general second-brain framing toward a GitHub sprint/deployment planner.
- Clarified product direction: primary purpose remains a second brain over work artifacts; sprint planning and deployment goal-setting are secondary outcomes, driven especially by deeper GitHub artifact ingestion and deploy-readiness inference.
- Hard requirement: do not rely on an agent skimming hundreds of live GitHub issues/PRs; ingest and vectorize GitHub artifacts into local SQLite so retrieval and recommendations are driven from the local store.
- The readiness layer should stay explicit and inspectable: computed status, confidence, evidence, and blockers from local repo signals instead of opaque hidden reasoning.
- Weekly review notes should be able to write back into the Obsidian vault as `week-of-YYYY-MM-DD.md` artifacts and immediately re-enter the retrieval pipeline so next week's knowledge includes last week's summary.
- Next planning priorities: add inferred GitHub issue<->PR reconciliation with high/medium-confidence close suggestions, and add Sleuth AI / Slack reminder ingestion as a structured signal source before broader Slack thread ingestion.
- The reviewed Sleuth reminders JSON is a list-shaped feed with fields such as `ReminderID`, `ReminderMessageText`, `ShouldPostOn`, `OriginalChannelID`, and `OriginalMessageID`; start Slack integration from this structured reminder feed before broader thread ingestion.
- Experimental next step: a deterministic GitHub Action should run every 2-3 days, emit JSON + Markdown close-candidate reports, and stay report-only until the weekly local agent review proves the thresholds.
- The git-pulse maintained codebase now lives under `experimental/git-pulse/`, with the plan at `experimental/git-pulse/git-pulse-plan.md`.
- The separate sync repo for cross-device pulse data can live outside the config dir via `sync_repo_dir`, currently intended to support a local checkout like `~/Documents/GitHub-Repos/rebalance-git-pulse`.
- Current operator-selected git-pulse sync checkout: `/Users/noelsaw/Documents/GH Repos/rebalance-git-pulse`.
- macOS `launchd` can be denied unattended write access to a `sync_repo_dir` under `~/Documents`; the safer pattern is a real repo in an unprotected path plus an optional symlink for convenience.
- The git-pulse README should treat `~/.config/git-pulse/repo` or another non-protected path as the recommended `sync_repo_dir` for cross-Mac installs; `~/Documents` is not reliable for unattended launchd writes.
- On macOS, launchd may be blocked from executing scripts inside `~/Documents`, `~/Desktop`, or `~/Downloads`; git-pulse should install a copied `~/bin/git-pulse` launcher in that case instead of a symlink.
- Local operator convention: `~/bin/git-pulse.log` should point to `~/.config/git-pulse/logs/git-pulse.err` for quick log inspection.
- git-pulse canonical storage now uses `device_id`-keyed pulse files with UTC timestamps plus `devices/*.yaml` metadata, and `~/bin/git-pulse-view` is the local unified reader.
- Fresh installs on a new Mac need either a pre-existing `sync_repo_dir` checkout or a valid `sync_repo` remote URL plus working GitHub auth; `gh auth status` can be stale/invalid even when no local git-pulse config exists yet.
- `experimental/git-pulse/install.sh` must treat blank `device_id` and `device_name` values the same as missing ones, and must shell-escape quoted Mac computer names when writing `config.sh`.
- `git-pulse-view` should favor one canonical TSV output for both humans and machines: `local_day`, `local_time`, `utc_time`, `device_id`, `device_name`, `repo`, `branch`, `short_sha`, `subject`, with range filtering such as `--days 14` instead of comment-heavy preambles.
- A reusable combined git pulse report means a saved file generated by the productized CLI, not a transient terminal merge; `git-pulse-view --days N --include-local-unsynced --output <path>` is the right shape.
- git-pulse also needs a second-layer Markdown recap over saved TSV reports; the useful contract is a de-duplicated all-machines rollup with summary metrics, coverage, repo/device tables, daily activity, recent activity, and exception flags.
- The current git-pulse planning direction is to use `/Users/noelsaw/Documents/GH Repos/rebalance-git-pulse` as the raw historical data root and add a derived SQLite history layer on top, preferably under a non-committed `derived/` path in that checkout.
- Human-friendly git-pulse ids should use trimmed slug names like `noels-mbp-16-m1-pro`, dropping apostrophes entirely instead of turning them into `-s-`.
- The hourly `collect.sh` path should auto-migrate a machine's own legacy git-pulse ids, including prior UUID defaults and pre-apostrophe-fix slugs, so cleanup does not require a manual agent pass on each device.
- The collector-side legacy-id migration must stage deletions with `git add -A -- <old> <new>`; otherwise stale machine pulse/metadata files survive in the sync repo and keep duplicate devices visible.
- The copied `~/bin` Python git-pulse launchers need their support files installed beside them; `pulse_common.py` is required for imports, and the recap skill markdown files should travel too so copied-launcher paths stay valid on machines using copy mode from `~/Documents`.
- Real-world git-pulse recap output surfaced stale sync-repo metadata for legacy device ids; coverage should flag those mismatches instead of silently hiding them.
- On Noel's MacBook Pro 14, `sync_repo_dir` is already safe at `~/.config/git-pulse/repo`, but the watched repos still live under `~/Documents/GitHub Repos`, so launchd-triggered git-pulse needs `/bin/bash` granted Full Disk Access or the repos moved out of `~/Documents`; after recovery, `last-run` should be rolled back at least to `2026-04-22T15:13:18Z` to re-collect the broken window.
- Granting `/bin/bash` Full Disk Access on Noel's MacBook Pro 14 fixed the launchd collector; after kickstart on 2026-04-22 local evening, the machine resumed pushing and re-collected the missing window, including a recovered `rebalance-OS` entry at `2026-04-22T15:55:34Z`.
- Local operator convenience: keep a repo-root `git-pulse-sync` symlink pointing at `~/.config/git-pulse/repo`, and ignore that symlink in `.gitignore` so the sync checkout is easy to reach without digging through dot-config paths.
- On Noel's Mac Studio, git-pulse should auto-discover local GitHub repos from `~/Documents/GH Repos` and `~/Documents` during install, persist the merged `repos=()` list into `~/.config/git-pulse/config.sh`, and prefer local filesystem discovery over `gh` CLI for watched-repo scope.
- The unified semantic index now lives in `semantic_documents` + `semantic_embeddings` + `semantic_embedding_meta`; vault ingest and GitHub artifact sync dual-write into it, while source-native tables remain canonical.
- GitHub ingest now supports an operator-local exact repo ignore list in `temp/rbos.config` under `github_ignored_repos`; the intended workflow is add-with-purge so ignored repos like `dlt-hub/dlt` disappear from future GitHub scans, artifact syncs, and GitHub semantic backfills.
- GitHub triage now supports central tracker repos with related/affiliate implementation repos via `temp/rbos.config` under `github_related_repos`; use it when Universal Theme issues are the project hub but plugin repo issues/PRs hold implementation work.
- Pulse Sleuth scope should include reminders assigned to the operator and reminders assigned by the operator to others; `slack_user_id` matches both `assignee_id` and `original_sender_id`.
- Successful full `refresh_index(scope=["all"])` runs should refresh `Dashboards/rebalanceOS Dashboard.md` in the configured vault and re-ingest/embed the note so the dashboard is visible in Obsidian and current in SQLite.
- Operator-local project/client priority rules live in gitignored `temp/rbos.config` under `project_priority_rules`; dashboards overlay them onto project registry rows and can add virtual priority rows before a project exists in the registry.
- The project registry can now be bootstrapped from local SQLite signals with `rebalance ingest infer-project-registry`; the current heuristic merges org-style GitHub owners like `NeochromeTeam`/`BinoidCBD` into umbrella projects and supplements them with recurring calendar-only projects such as `LTVera`, `MacNerd`, and `MyFriend.com`.
- Product-surface direction: Obsidian and the vault should be the primary dashboard and note-entry surface, while VS Code agent chats stay the deep-work surface for implementation, debugging, and one-off audits.
- The next operator-facing artifact should be a synthesized `rebalanceOS Dashboard` markdown note written into the Obsidian vault, driven from recent highlighted work and designed so deterministic local rendering can optionally be upgraded with an agent/LLM narrative pass.
- ask-self integration should remain external: keep repo-local harness files under `ask_self/`, wrapper scripts under `scripts/`, and rely on `ASK_SELF_PATH` instead of vendoring or submoduling the scanner.
- This repo's portable ask-self ingest wrapper should default to full-corpus (`--mode all`) and fail fast when PR ingestion is requested without working GitHub auth; a "successful" portable refresh that silently drops the remote PR slice is not acceptable.
- On macOS Bash with `set -u`, the portable `scripts/ask-self-ingest.sh` wrapper must not splat an empty optional array directly into `exec`; build the argv incrementally or the refresh dies before ask-self starts with `DEFAULT_ARGS[@]: unbound variable`.
- The committed portable ask-self index for this repo now uses `ask_self/ask_self_harness.json` `db_path: "ask_self/index/rebalance-OS.sqlite"` plus local `qwen-local` embeddings (`Qwen/Qwen3-Embedding-0.6B`, 1024 dims); on Mac the repo wrapper itself now enforces the stable refresh defaults (`TOKENIZERS_PARALLELISM=false`, `ASK_SELF_QWEN_BATCH_SIZE=8`, `ASK_SELF_QWEN_MAX_TOKENS=2048`, `--concurrency 1`) so maintainers do not have to remember them manually. The 2026-05-28 refresh rebuilt `1430` chunks into an ~11 MB tracked SQLite DB and kept the sidecars gitignored.
- On Noel's MacBook Pro 14, rebalance OS now uses the canonical live DB at `~/Library/Application Support/rebalance-os/rebalance.db`, has the vault path `/Users/noelsaw/Documents/Noel Saw`, persists the GitHub token in repo-local `temp/rbos.config`, and has the five core launchd jobs loaded (`daily-sync`, `vault-sync`, `github-sync`, `pulse-sync`, `pulse-web-sync`); doctor is green except for the optional missing Sleuth env file.
- Background launchd collectors and local MCP clients must run against the repo checkout's `src/` tree, not the wheel copy in `.venv/site-packages`; otherwise config discovery drifts into `.venv/lib/.../temp/rbos.config` and the jobs lose access to repo-local `temp/rbos.config`.
- The Pulse web page does not use an icon font library; banner actions should prefer inline SVG icons so the static page stays dependency-light apart from the existing Chart.js include.
- On Noel's current machine, Pulse publish should target `github_login=noelsaw1` and `pulse_target_path=~/.config/git-pulse/repo`; Gmail ingest is intentionally set to MCP mode in `temp/rbos.config`.
- In headless Codex sessions, this repo's portable `ask_self` query path can be unavailable even when the committed index exists: `qwen-mlx` needs Metal access, and `qwen-local` needs a cached Hugging Face model or network access. Fall back to the local `ask_self/index/rebalance-OS.sqlite` corpus plus targeted source files instead of broad grep.
- On Noel's current machine, a working local `ask_self` retrieval run from this repo needed `ASK_SELF_ENABLE_QWEN=1 ASK_SELF_PYTHON=.venv/bin/python bash scripts/ask-self-query.sh ...` outside the sandbox; the default resolver otherwise picked a Python/runtime combination that either missed `mlx-embeddings` or had no Metal access.
- The Stickies importer lives under `utils/stickies-to-obsidian/`; keep `DRY_RUN=true` side-effect free for the Obsidian target and state ledger, and keep the launchd entry separate from the core rebalance schedulers.
- On Noel's current machine, the Stickies-to-Obsidian launchd agent is installed as `~/Library/LaunchAgents/com.user.stickies2obsidian.plist`, targeting `/Users/noelsaw/Documents/Noel Saw/Stickies.md` with state ledger `~/.stickies2obsidian.state`.
- On Noel's MacBook Pro 14, the local pulse dashboard server on `http://127.0.0.1:8767` runs as launchd job `com.rebalance-os.pulse-server`; if it fails with `ModuleNotFoundError: fastapi`, install the repo's optional server runtime into `.venv` with `pip install '.[server]'` and reload `scripts/install_pulse_server_scheduler.sh`.
- `scripts/pulse_warning_watch.py` is the local pulse-banner watcher: it curls `http://127.0.0.1:8767/`, extracts the top collector warning banner, logs JSONL to `temp/pulse-warning-watch.jsonl`, and can optionally ask an OpenAI-compatible local model such as Ollama/Gemma for a short triage note.
- `git-pulse-health` needs a sync-visible collector heartbeat, not just pulse-file git history; the right contract is to write `last_scan_epoch` / `last_scan_utc` into each device YAML on successful collector runs and have health checks prefer that heartbeat while still surfacing pulse-update age and local-commit age as separate context.
- git-pulse collector scans can succeed partially: preserve successful repo rows, record `repo_scan_failures` plus `scan_failure_examples` in device metadata, classify that state as `DEGRADED` in `git-pulse-health`, and do not advance `last-run` until every watched repo scan succeeds.
- Project plan docs should prefer frontmatter, a 2-column status table (What was just completed and What's next), a Table of Contents, and phased observable checklists.
- Focus 5 product decisions are locked for now: rank by local repo activity, require zero manual repo config, and treat repos active at session start or during the session as in scope.
- Focus 5 architecture direction shifted from a server-side "session" concept to a persisted roster snapshot with TTL; top-5 git health is live, off-roster warning inputs should be cached/persisted, and local git data is the primary signal with GitHub as enrichment.
- Write-path audit on 2026-06-10: `index_ops.py` is the intended orchestrator, but the collector registry still mixes raw ingress sources (`vault`, `github`, `calendar`, `sleuth`, `email`, `figma`) with derived/export scopes (`semantic`, `sync`, `code`, `focus5`, `ask_self`), and many MCP/CLI write surfaces still bypass `refresh_index()` and call source-specific ingest functions directly.
- Portability hotspots that materially affect collector refactor planning: repo-local `temp/rbos.config` remains a primary config store, Calendar/Gmail token fallbacks still hardcode `~/.config/rebalance-os/...`, runtime code still has repo-root assumptions (`parents[3]`, `sys.path.insert`), and Sleuth grouping still assumes a sibling `~/Documents/GH Repos/sleuth-app` checkout.
- On 2026-06-10, `ARCHITECTURE.md` was corrected to match the current code: it now distinguishes raw sources from other registered collector scopes, documents the current `all` expansion, adds Figma/email/ask_self/focus5 coverage, fixes the GitHub/Gmail credential story, and updates the module map away from the old monolithic `cli.py` / `db.py` wording.
- Auth/keyring trace on 2026-06-20: the original keyring project mostly shipped as a keyring-primary plus launchd-fallback credential model, but the superseding hardening follow-up now lives at `PROJECT/2-WORKING/AUTH-AND-API-KEY-STORAGE-HARDENING.md`; that doc points back to `UPGRADE.md` and `PROJECT/1-INBOX/SUBSYSTEM-REFACTOR.md` as the older shipped-model references.
- As of 2026-06-21, the canonical active sequencing doc for the remaining front-door onboarding, collector portability, and auth hardening follow-up work is `PROJECT/2-WORKING/FRONT-DOOR-PORTABILITY-AUTH-UNIFICATION.md`; ROADMAP should point there rather than splitting this area across the three older project plans.
- As of 2026-06-25, the canonical standalone Mac track for Focus 5 lives in `PROJECT/2-WORKING/FOCUS-5-APP-STORE.md` and requires a fully native App Store-submittable runtime with no rebalance-OS, Python, localhost-server, or repo-script dependency.
- As of 2026-06-25, PDDA's durable runtime surface is `utils/pdda.sh` for deterministic checks plus separate `utils/pdda-doc-ready.sh` for the opt-in LLM layer; do not add wrapper commands unless a real external integration forces them, and treat `PROJECT/1-INBOX/GH-*.md` roadmap parking as a first-class `roadmap-coverage` rule.
- As of 2026-06-26, the Apple Reminders unified plan carries a deferred optional write-back phase: borrow dry-run/apply, backup, mock-harness, and Swift-bridge patterns from `text-replacement-studio-macos`, but do not reuse its direct table-mutation logic or treat SQLite as the primary Reminders write surface.
