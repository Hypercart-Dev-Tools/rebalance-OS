Why it's called 4X4 
4 Domains: Strategic Goals, Current Goals, Past Goals, Lessons Learned x 4 Tasks

Keep this file short and sweet. Enough details but barely enough to point in the right direction.

Start with only 4 weekly goals. Cross those off first before adding more.

PROJECT OVERVIEW
rebalance-OS is a local-first project focus system built around an Obsidian vault source of truth, a Python ingest pipeline, and MCP tools for host integrations.
This week is about tightening project signal quality: keep discovery automatic, prioritize active work, and make the registry trustworthy enough for daily decision support.

PROJECT ANTI-GOALS (OPTIONAL)
1. Do not overbuild the scoring model before ingestion and segmentation are stable.
2. Do not add cloud dependencies for core functionality.
3. Do not mix adapter-specific behavior into the MCP server core.
4. Do not optimize UI/report polish until core data quality is reliable.

A. STRATEGIC GOALS
1. Ship a stable, repeatable preflight pipeline that converts vault and GitHub signals into clean registry sections.
2. Keep the in-vault markdown registry as the canonical source of truth with safe sync to SQLite/YAML projections.
3. Expand MCP tools from read-only listing toward practical daily triage (activity balance, focus cues, next actions).
4. Maintain security and portability: minimal permissions, local data by default, and a clean upgrade path for secrets handling.

B. CURRENT WEEK GOALS
1. Add a one-time re-segmentation command to bucket existing potential projects by `last_activity_at`.
2. Run preflight + sync smoke test end-to-end and confirm markdown, YAML projection, and SQLite stay consistent.
3. Add/verify MCP tool output for segmented project sections to support host-side prioritization.
4. Rotate exposed API keys found in notes and confirm PAT handling remains read-only and local.

C. LAST WEEKS ACCOMPLISHMENTS
1. Reviewed and fixed key README/PROJECT red flags (links, command clarity, scheduler framing, MCP genericity).
2. Reworked architecture to use in-vault markdown registry as canonical source, with sync modes pull/push/check.
3. Ported GitHub activity scanner from gitdaily to Python and integrated CLI + SQLite persistence + MCP `github_balance`.
4. Implemented activity segmentation logic in preflight (`most_likely_active`, `semi_active`, `dormant`, fallback potential).

D. RECENT LESSONS LEARNED
1. Better defaults beat clever pipelines: simple non-interactive paths and clean markdown output accelerate trust.
2. Source-of-truth decisions early (vault markdown first) prevent expensive refactors later.
3. Activity signals are useful but incomplete; keep a fallback bucket for projects without GitHub footprints.
4. Security hygiene cannot wait: plaintext keys in notes are a real operational risk and need immediate rotation.