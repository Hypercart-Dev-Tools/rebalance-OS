PROJECT OVERVIEW
rebalance-OS is a local-first second brain over work artifacts, centered on an Obsidian vault, SQLite/sqlite-vec, and MCP/CLI interfaces.
The current push is to turn that evidence layer into a calm weekly operating system: Obsidian as the dashboard and note-entry surface, SQLite as the canonical attention ledger, and a narrower verdict-first MCP surface that tells you where attention went, where it should go, and what to rebalance next.

PROJECT ANTI-GOALS (OPTIONAL)
1. Do not rely on live-skimming hundreds of GitHub artifacts at answer time.
2. Do not move core knowledge workflows to cloud-hosted storage or embedding services.
3. Do not hide project-state reasoning behind opaque inference with no evidence trail.
4. Do not widen ingest surfaces again until the weekly rebalance loop is clearly useful inside Obsidian.

A. STRATEGIC GOALS
1. Make Obsidian the primary dashboard, review, and note-entry surface while generated writebacks flow back into the vault.
2. Build a trustworthy SQLite attention ledger across calendar, git-pulse, GitHub, reminders, and vault signals.
3. Turn multi-source signals into explicit weekly rebalance verdicts with evidence, confidence, and next moves per project.
4. Preserve local-first portability and a narrow operator surface: verdict-first MCP tools for dashboard workflows, broader retrieval tools only for drilldown.

B. CURRENT WEEK GOALS
1. [ ] Generate the first Obsidian weekly rebalance note from current data with `verdict`, `evidence`, and `next_move` per project.
2. [ ] Define and start implementing the attention ledger contract: `project_targets`, `attention_events`, `attention_feedback`, and weekly rollup views.
3. [ ] Narrow the MCP surface toward `weekly_rebalance`, `project_attention`, and unattributed-review workflows while keeping `ask` as exploratory only.
4. [ ] Stabilize the live operator loop on this Mac: canonical app-data DB, doctor-green schedulers, and one clear recovery path for optional Sleuth gaps.

C. LAST WEEKS ACCOMPLISHMENTS
1. Brought back a real health-and-onboarding path: `rebalance doctor` and `rebalance onboard` now exist again on `development`, with integration credential checks and more honest operator-state reporting.
2. Repaired local operator portability end-to-end: the canonical app-data DB path, templated launchd installers, path-derived scripts, and ask-self wrapper hardening now let this repo boot cleanly on a fresh machine without hand-editing `/Users/<name>/...` paths.
3. Recovered this Mac's live rebalance install: canonical DB migrated to `~/Library/Application Support/rebalance-os/rebalance.db`, vault + GitHub token persisted into config, 14 projects registered, and the five core launchd jobs loaded and doctor-green.
4. Tightened GitHub ingest ergonomics: explicit public-repo artifact sync no longer requires a PAT, and the portable ask-self ingest wrapper now defaults to full-corpus mode and fails fast instead of silently dropping the remote PR slice.

D. RECENT LESSONS LEARNED
1. Broad retrieval is useful for exploration, but a second brain needs verdict-first outputs to stay useful day to day.
2. Obsidian is the right operator surface, but computation and normalization must stay in SQLite plus MCP or CLI layers.
3. Exact attribution and low-noise classification matter more than adding another signal source.
4. Operator-portability is not a one-shot cleanup. Paths, auth reachability, canonical DB selection, and scheduler state all regress unless the repo keeps explicit health tooling and baseline-aware drift checks.
