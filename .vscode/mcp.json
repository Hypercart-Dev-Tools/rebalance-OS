{
  "servers": {
    "rebalance": {
      "type": "stdio",
      "command": "${workspaceFolder}/.venv/bin/python",
      "args": ["-m", "rebalance.mcp_server"],
      "env": {
        "REBALANCE_DB": "${workspaceFolder}/rebalance.db"
      }
    }
  }
}
